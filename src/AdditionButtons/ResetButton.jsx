import React from 'react';

const ResetButton = ({ onClick }) => {
    return (
        <button onClick={onClick} className="each-button2">
            Reset Array
        </button>
    );
};

export default ResetButton;
